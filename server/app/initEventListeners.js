// Initialize all event listeners
// This function initializes all event listeners in the application
// The event listeners are used to listen for events from Redis, the database, and the Asterisk Manager Interface (AMI)
// The event listeners are initialized in the server.js file
// The event listeners are used to listen for events and log them to the console
// The event listeners are used to listen for events and perform other actions in the application
// The event listeners are used to listen for events and update the application state
// The event listeners are used to listen for events and send notifications to users
// The event listeners are used to listen for events and perform other actions in the application

  // import { initializeRedisEvents } from '../events/redisEvents.js';
  // // import { initializeDatabaseEvents } from '../events/dbEvents.js';
  // import { initializeAmiEvents } from '../events/amiEvents.js';
  
  // export default function initEventListeners() {
  //   initializeRedisEvents();
  //   // initializeDatabaseEvents();
  //   initializeAmiEvents();
  // }